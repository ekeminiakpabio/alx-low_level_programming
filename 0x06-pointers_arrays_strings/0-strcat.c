#include "main.h"

/**
 * _strcat - concatenator of strings
 * @dest: copied to
 * @src: copied from
 * Return: concatenation
 */
char *_strcat(char *dest, char *src)
{
	int k, i;

	for (k = 0; dest[k] != '\0'; k++)
	{
	}

	for (k = 0; (dest[k + i] = *src++) != '\0'; n++)
	{
	}

	return (dest);
}
