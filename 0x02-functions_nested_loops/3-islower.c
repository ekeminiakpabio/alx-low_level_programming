#include "main.h"
/**
 * _islower - lower than
 * @c: character
 * Return: 0 or 1 depends on return value
 *
 */

int _islower(int c)
{
	if (c >= 'a'; && c <= 'z')
		return (1);

	else
		return (0);
}
